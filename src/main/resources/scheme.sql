CREATE TABLE clients (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         name VARCHAR(255),
                         department VARCHAR(255),
                         salary DECIMAL
);
